/// Range: [0f,1f]
typedef Ratio = double;

/// Range: [0f,1f]
typedef Progress = double;

/// No range limited
typedef Distance = double;

/// the number of repeating
typedef Times = int;

typedef Matcher<T> = bool Function(T e);